node_modules/
dist/
.env
.env.local

# SQLite database and uploaded photos are local state, not source.
server/data/
server/uploads/listings/

.DS_Store
*.log
dist-demo/

# Impeccable review captures and build scratch (PRODUCT.md, config and surface briefs are tracked)
client/.impeccable/review/
client/.impeccable/build/
client/.impeccable/mocks/
client/.impeccable/live/
client/.impeccable/config.local.json
