# Google sign-in configuration comes from /api/auth/config.
# Set GOOGLE_CLIENT_ID in the server/root .env, not in this file.
# Prefer serving the frontend and API from one origin. See ACCOUNTS.md.
# VITE_API_BASE_URL=https://api.your-domain.com
