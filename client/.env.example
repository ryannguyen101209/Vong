# Google Identity Services web client ID.
# Create an OAuth 2.0 Web application in Google Cloud Console, then add the
# local and production origins. The frontend can render the official Google
# button with this value; the backend phase must still verify every credential.
VITE_GOOGLE_CLIENT_ID=
